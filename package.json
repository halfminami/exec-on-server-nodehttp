{
  "name": "exec-on-server-nodehttp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc-server":"npx tsc -p ./server/tsconfig.json",
    "tsc-src":"npx tsc -p ./src/tsconfig.json",
    "tsc-all": "npm run tsc-server && npm run tsc-src",
    "serve": "node ./server/JS/server.js"
  },
  "author": "",
  "license": "ISC",
  "private": "true",
  "devDependencies": {
    "@types/node": "^18.15.12",
    "typescript": "^5.0.4"
  }
}
